# EA Builder Workflow - Powered by OpenAI GPT-5.2 mini

เว็บแอปพลิเคชันสำหรับสร้างสถาปัตยกรรมองค์กร (Enterprise Architecture) แบบครบวงจร โดยใช้พลังของ **OpenAI GPT-5.2 mini** เพื่อวิเคราะห์บริบท ออกแบบวิสัยทัศน์ และสร้าง Roadmap อัตโนมัติผ่าน 6 ขั้นตอนหลัก

## ✨ ไฮไลต์สำคัญ
- 🧠 **AI-Powered Architecture** – ใช้ OpenAI GPT-5.2 mini ช่วยสังเคราะห์เนื้อหา EA ที่สัมพันธ์กับข้อมูลที่ผู้ใช้กรอก
- 🔒 **Workflow ปลอดภัย** – JWT Authentication + Netlify Functions พร้อม Rate limiting
- 📊 **6 ขั้นตอน EA Workflow** – Context → Direction → Blueprint → Roadmap → Cybersecurity → Output & Monitoring
- 📤 **Export ง่าย** – ดาวน์โหลด TXT, พิมพ์เป็น PDF, บันทึกอัตโนมัติทุก 30 วินาที
- 📱 **Responsive + PWA** – รองรับทุกหน้าจอและติดตั้งเป็นแอปได้

## 🛠️ เทคโนโลยี
- **Frontend**: HTML5 + Tailwind CSS + Vanilla JavaScript
- **AI Integration**: OpenAI Responses API (GPT-5.2 mini)
- **Backend**: Netlify Functions (`login`, `validate`, `openai`)
- **Auth**: JSON Web Tokens (JWT)
- **Config**: `config.js` สำหรับกำหนด API Base URL/Host override

## ⚙️ การตั้งค่าเริ่มต้น

### 1. ติดตั้ง dependencies (ใช้เฉพาะ dev env ที่ต้องการ Netlify CLI)
```bash
npm install
```

### 2. ตั้งค่า Environment Variables บน Netlify
| Key | Value | อธิบาย |
| --- | --- | --- |
| `JWT_SECRET` | ค่า secret | ใช้เซ็น JWT tokens |
| `OPENAI_API_KEY` | `sk-...` จาก [platform.openai.com](https://platform.openai.com/) | ต้องมีสิทธิ์เรียก GPT-5.2 mini |
| `OPENAI_MODEL` *(ไม่บังคับ)* | `gpt-5-mini` | ใช้ override ชื่อโมเดลหากต้องการ |

> ฟังก์ชัน `netlify/functions/openai.js` จะตรวจสอบ API Key ก่อนเรียก OpenAI Responses API

### 3. ทดสอบไฟล์ Static
```bash
npm run dev   # ใช้ Netlify dev (ต้อง login Netlify CLI)
# หรือ
npm run serve # เปิด HTTP server ทั่วไป (ตอบสนองเฉพาะหน้า static)
```

## 🧩 สถาปัตยกรรมการเรียก OpenAI

คำขอฝั่ง frontend จะเรียก Netlify Function `/api/openai` (ผ่าน `callOpenAI()` ภายใน `index.html`) โดยฟังก์ชันนี้จะแปลงข้อความที่ผู้ใช้ป้อนให้เป็น payload ของ OpenAI Responses API ตามโครงสร้างต่อไปนี้:

```json
{
  "model": "gpt-5-mini",
  "input": [
    { "role": "system", "content": [{ "type": "text", "text": "<OPENAI_SYSTEM_PROMPT>" }] },
    { "role": "user", "content": [{ "type": "text", "text": "<PROMPT>" }] }
  ],
  "text": { "format": { "type": "text" }, "verbosity": "medium" },
  "reasoning": { "effort": "medium", "summary": "auto" },
  "tools": [],
  "store": true,
  "include": [
    "reasoning.encrypted_content",
    "web_search_call.action.sources"
  ],
  "max_output_tokens": 1200
}
```

คำตอบที่ได้จะถูก flatten แล้วส่งกลับมายัง frontend เพื่อกรอกข้อมูลในแต่ละฟิลด์ (หรือ JSON fields ที่ผู้ใช้ร้องขอ)

## 🔐 ระบบ Login + API Base URL

- หน้า `login.html` จะเชื่อมต่อ Netlify Functions (`/api/login`, `/api/validate`)
- หากรันแบบ offline (`localhost`) จะมี user mock ให้ใช้งาน
- ผู้ใช้สามารถตั้งค่า API Base URL ได้จากหน้า login เพื่อเชื่อมกับ Netlify Functions บนโดเมนอื่น (ค่าเก็บใน `localStorage: ea_api_base_url`)

## 📦 โฟลเดอร์สำคัญ
- `index.html` – แอปหลักที่ผสาน OpenAI + Workflow UI
- `login.html` – หน้าเข้าสู่ระบบ
- `ea.html` – เวอร์ชันสรุป/ทดลองที่ให้กำหนด OpenAI Key เอง
- `config.js` – Utility สำหรับกำหนด API Base URL และ host override
- `netlify/functions/openai.js` – Proxy เรียก OpenAI Responses API พร้อม rate limiting
- `netlify/functions/login.js`, `netlify/functions/validate.js` – จัดการ JWT

## 🚀 Deploy
1. สร้างโปรเจ็กต์บน Netlify
2. กำหนด Environment Variables ตามตารางด้านบน
3. Deploy ผ่าน Git Integration หรือ `netlify deploy`
4. ถ้าจะโฮสต์หน้า static บน GitHub Pages/โดเมนอื่น ให้ตั้งค่า API Endpoint ในหน้า login เพื่อชี้กลับมาที่ Netlify Functions

## 🧰 การปรับแต่ง
- แก้ไขสีหลักได้ที่ตัวแปร CSS (`:root`) ใน `index.html`
- ถ้าต้องการเพิ่มขั้นตอน/โมดูลใหม่ ให้เพิ่ม UI และเรียก `callOpenAI()` พร้อม prompt เฉพาะ
- ใช้ `window.EA_CONFIG.hostOverrides` ใน `config.js` เพื่อ map hostname → API Base URL อัตโนมัติ

## 🐞 การแก้ปัญหาเบื้องต้น
1. **OpenAI API error** – ตรวจสอบ quota หรือยืดช่วงเวลาในการกดปุ่ม Auto
2. **401 Unauthorized** – Token login หมดอายุ ให้ login ใหม่
3. **ค้างที่หน้า Login** – เช็กว่า API Base URL ชี้ไปยัง Netlify (สำหรับ GitHub Pages)

## 📄 License
MIT License

---

**Developed by Wirun Wetsiri**  
*Powered by OpenAI GPT-5.2 mini*

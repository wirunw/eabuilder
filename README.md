# EA Builder Workflow - Powered by Typhoon AI

เว็บแอปพลิเคชันสำหรับสร้างสถาปัตยกรรมองค์กร (Enterprise Architecture) อัจฉริยะ ผสานพลังของ **Typhoon AI (SCB 10X)**

## ✨ คุณสมบัติหลัก

### 🧠 AI-Powered Architecture
- ใช้ Typhoon AI ช่วยวิเคราะห์และสร้างเนื้อหาอัตโนมัติ
- รองรับ 6 ขั้นตอนการสร้าง EA แบบครบวงจร
- Context-aware AI ที่เรียนรู้จากข้อมูลที่ผู้ใช้ป้อน

### 🔒 ความปลอดภัยสูง
- API Key จัดเก็บในเครื่อง (Local Storage) เท่านั้น
- ไม่มีการส่งข้อมูลไปยังเซิร์ฟเวอร์อื่น
- Security headers ครบครันสำหรับการ Deploy

### 📱 Responsive Design
- รองรับทุกขนาดหน้าจอ (Mobile, Tablet, Desktop)
- Progressive Web App (PWA) พร้อมติดตั้งบนมือถือ
- Dark mode support

### 📊 6 ขั้นตอน EA Workflow
1. **Context Analysis** - วิเคราะห์ SWOT และบริบทองค์กร
2. **Direction Management** - กำหนดวิสัยทัศน์และหลักการ
3. **Blueprint Management** - ออกแบบ Business & Tech Architecture
4. **Roadmap Management** - วางแผน Timeline และ KPIs
5. **Cybersecurity** - วิเคราะห์ความปลอดภัยและ Governance
6. **Output & Monitor** - สรุปรายงานและ Export

### 🚀 Export & Sharing
- ส่งออกเป็นไฟล์ TXT
- พิมพ์/บันทึกเป็น PDF
- Auto-save ทุก 30 วินาที
- Sample data สำหรับทดสอบ

## 🛠️ เทคโนโลยีที่ใช้

- **Frontend**: HTML5, Tailwind CSS, Vanilla JavaScript
- **AI Integration**: Typhoon AI API (SCB 10X)
- **Icons**: Font Awesome 6
- **Fonts**: Google Fonts (Sarabun + Inter)
- **Deployment**: Netlify (Static Site)

## 📋 การติดตั้งและใช้งาน

### 1. การตั้งค่า Typhoon API Key
1. คลิกปุ่ม Settings (⚙️) มุมขวาบน
2. กรอก API Key จาก [opentyphoon.ai](https://opentyphoon.ai)
3. คลิก "บันทึก" - API Key จะถูกจัดเก็บในเครื่องเท่านั้น

### 2. การใช้งาน
1. เริ่มต้นที่หน้า Welcome แล้วคลิก "เริ่มสร้าง Architecture"
2. กรอกข้อมูลองค์กรใน Phase 1 (Context Analysis)
3. ใช้ปุ่ม AI ช่วยสร้างเนื้อหาอัตโนมัติ
4. ดำเนินการไปตามขั้นตอน A-F จนถึง Phase 6
5. ส่งออกรายงานใน Phase 6

### 3. Keyboard Shortcuts
- `Ctrl/Cmd + S` - บันทึกรายงาน (Phase 6)
- `Ctrl/Cmd + P` - พิมพ์/Save as PDF
- `Ctrl/Cmd + ,` - เปิด Settings
- `Esc` - ปิด Modal

## 🚀 การ Deploy บน Netlify

### วิธีที่ 1: Drag & Drop
1. ไปที่ [Netlify](https://netlify.com)
2. ลากโฟลเดอร์โปรเจคไปวางในหน้า Deploy
3. รอสักครู่ - เว็บไซต์พร้อมใช้งาน!

### วิธีที่ 2: Git Integration
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <your-repo-url>
git push origin main
```

จากนั้นเชื่อมต่อ repository กับ Netlify

### ไฟล์ที่จำเป็นสำหรับ Deploy
- `index.html` - หน้าหลัก
- `manifest.json` - PWA configuration
- `netlify.toml` - Netlify settings & security headers

## 🌐 การใช้งานบน GitHub Pages หรือโดเมนอื่น

ระบบ Login และ Typhoon API ทำงานอยู่บน Netlify Functions ดังนั้นถ้าโฮสต์ไฟล์หน้าเว็บไว้บน GitHub Pages (หรือ Static Host อื่น) ต้องชี้ API กลับมาที่ Netlify เสมอ

1. Deploy โค้ดไปยัง Netlify ผ่าน Git/CLI เพื่อให้ Functions ถูกรวมและติดตั้ง dependencies ให้อัตโนมัติ (การ Drag & Drop ไม่รองรับ Functions)
2. เปิดหน้า `login.html` แล้วตั้งค่า **API Endpoint** ด้วย URL ของ Netlify site (เช่น `https://your-site.netlify.app`)
3. กด "บันทึกค่า API Base URL" ค่านี้จะถูกเก็บใน `localStorage` (key: `ea_api_base_url`)
4. จากนี้เว็บบน GitHub Pages จะเรียก Netlify Functions ผ่าน CORS ทำให้ Login และ Typhoon API ใช้งานได้เหมือนบน Netlify

> สามารถตั้งค่าแบบถาวรได้ที่ไฟล์ `config.js` (ตัวอย่าง: ตั้ง `hostOverrides` ให้ map hostname ของ GitHub Pages → Netlify URL)

## 🔧 การปรับแต่ง

### การเปลี่ยนสีธีม
แก้ไข CSS variables ใน `:root`:
```css
:root {
    --primary-color: #dc2626; /* เปลี่ยนสีหลัก */
    --secondary-color: #3b82f6; /* เปลี่ยนสีรอง */
}
```

### การเพิ่มฟีเจอร์ใหม่
- แก้ไขใน `index.html`
- เพิ่ม JavaScript functions ในส่วน `<script>`
- ใช้ `callTyphoon()` สำหรับเรียก AI API
- หาก Typhoon บัญชีของคุณใช้ชื่อโมเดลไม่ตรงกับค่าเริ่มต้น ให้ตั้งค่า environment variable `TYPHOON_MODEL` บน Netlify (เช่น `typhoon-1.5` หรือชื่อที่ Typhoon Dashboard แสดง) จากนั้น redeploy เพื่อให้ฟังก์ชัน `netlify/functions/typhoon.js` ใช้โมเดลที่ถูกต้อง

### การตั้งค่า `config.js`
- ใช้สำหรับกำหนดค่า API Endpoint และ host override
- ค่าเริ่มต้นจะเรียก API จากโดเมนเดียวกับเว็บ (เหมาะกับ Netlify)
- หากต้องการ map hostname เฉพาะ ให้เพิ่มใน `hostOverrides` เช่น:
```js
window.EA_CONFIG = {
  hostOverrides: {
    'wirunw.github.io': 'https://ea-builder.netlify.app'
  }
};
```
- ผู้ใช้สามารถแก้ไขค่าได้จากหน้า Login ผ่านฟอร์ม **API Endpoint** (จัดเก็บใน `localStorage`)

## 🐛 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย
1. **API Key ไม่ทำงาน** - ตรวจสอบว่ากรอกถูกต้องและยังไม่หมดอายุ
2. **AI ไม่ตอบ** - ตรวจสอบการเชื่อมต่อ internet และ API limits
3. **ข้อมูลหาย** - ตรวจสอบว่าเปิดใช้งาน Local Storage ใน browser

### Debug Mode
เปิด Developer Console (F12) และดูใน Console tab สำหรับ error messages

## 📄 License

MIT License - สามารถนำไปพัฒนาต่อได้

## 🤝 การมีส่วนร่วม

ยินดีรับ Pull Request และ Issue สำหรับพัฒนาต่อ

---

**Developed by Wirun Wetsiri**  
*Powered by Typhoon AI (SCB 10X)*
